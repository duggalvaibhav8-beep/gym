name: PHP Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on:  ubuntu-latest

    steps:
      - name: Deploy to Ec2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.IP }} 
          username: karan
          key: ${{ secrets.PRIVATE_KEY }}
          script: |
            cd /var/www/html/gym
            git pull origin main
            pm2 delete phpgym || true
            pm2 start "php -S 0.0.0.0:8089 -t ." --name phpgym
            pm2 save
